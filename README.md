# KADEORA
This web interface presents a minimal text environment where user input continuously accumulates in a single flowing stream. Instead of separating entries into lines or blocks, each input merges into an evolving body of text, shaped only by the screen’s width.
